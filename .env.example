# PostgreSQL (dev). Matches docker-compose.dev.yml.
DATABASE_URL="postgresql://hefesto:hefesto@localhost:5432/hefesto?schema=public"

# --- Deferred to a later increment (not used in v0.1 foundation) ---

# Auth0 (single admin user). Wire real login once a tenant is available.
# AUTH0_SECRET=
# AUTH0_BASE_URL=http://localhost:3000
# AUTH0_ISSUER_BASE_URL=
# AUTH0_CLIENT_ID=
# AUTH0_CLIENT_SECRET=

# Cloudflare R2 (blobs: receipts, photos). Only the URL is stored in the DB.
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET=
# R2_PUBLIC_URL=
